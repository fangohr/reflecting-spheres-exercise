language: cpp

python:
  - 3.5
compiler:
  - clang
  - gcc

script: make && make test


addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gcc-4.8
    - g++-4.8
    - clang
